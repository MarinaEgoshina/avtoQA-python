Баг -  репорт №1.
    Описание проблемы:

      Функция end_with класса StringUtils не корректно обрабатывает случай, когда передается пустая строка в качестве inputWord, а символ symbol не равен пустой строке.

Шаги воспроизведения:

Запустить тест test_end_with с параметрами: inputWord="", symbol="A", result=False.

ОР: Если входная строка inputWord пустая, а символ symbol не равен пустой строке, функция end_with должна вернуть False.
ФР: Функция end_with возвращает некорректное значение в данном случае, а именно True.

Окружение:

ОС: [windows 10]
Версия Python: [3.12.1]
Версия библиотек: pytest [8.0.0]
--------------------------------------------------------------------------------------------------------------------------
Баг - репорт №2.
    Описание проблемы:
      
      Описание проблемы:

      Функция starts_with класса StringUtils не корректно обрабатывает случай, когда передается пустая строка в качестве inputWord, а символ symbol не равен пустой строке.

Шаги воспроизведения:

Запустить тест test_starts_with с параметрами: inputWord="", symbol="A", result=False.

ОР: Если входная строка inputWord пустая, а символ symbol не равен пустой строке, функция end_with должна вернуть False.
ФР: Функция starts_with возвращает некорректное значение в данном случае, а именно True.

Окружение:

ОС: [windows 10]
Версия Python: [3.12.1]
Версия библиотек: pytest [8.0.0]
------------------------------------------------------------------------------------------------------------------------------
Баг - репорт №3.
      
      Описание проблемы:

      Функция is_empty класса StringUtils не  обрабатывает случай, когда передается None строка в качестве inputWord и возвращает Folse вместо True

Шаги воспроизведения:

Запустить тест is_empty с параметрами: inputWord=None,result=True.

ОР: Если входная строка inputWord=None, функция is_empty должна вернуть True.
ФР: Функция is_empty возвращает некорректное значение в данном случае, а именно Folse.

Окружение:

ОС: [windows 10]
Версия Python: [3.12.1]
Версия библиотек: pytest [8.0.0]

      